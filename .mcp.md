# MCP Server State — NEEDS REVIEW

**Status as of 2026-04-24:** All 3 MCP servers defined in `.mcp.json` are **DISABLED** via `.claude/settings.local.json` (`enabledMcpjsonServers: []`, all three moved to `disabledMcpjsonServers`).

## Why they're disabled

Chris reported that none of these were ever used for anything important:

| Server | Status | Notes |
|---|---|---|
| `insforge` | DISABLED | insforge.app BaaS. API key was committed to `.mcp.json` and pushed to GitHub. **Key has been rotated** (2026-04-24). Never used for real work. |
| `sqlite` | DISABLED | Read-only SQLite MCP for `backend/storage/app.db`. Replaced by running `sqlite3 backend/storage/app.db '...'` via Bash on demand — zero startup cost, works the same way. |
| `waifu-rt3d-api` | DISABLED | Local FastMCP bridge to backend. Unclear if still needed — depends on whether anything in `backend/mcp_bridge.py` is actively used in current workflow. |

## Questions for a future Claude Code session

Chris wants a future session to deliberately re-evaluate these. When you pick this up:

1. **Do any of these servers actually solve a problem Bash can't?** (MCP is for capabilities Bash can't express — streaming, structured introspection, auth flows. Read-only SQL is not one of those.)
2. **Is `backend/mcp_bridge.py` still in use?** If not, delete both the module AND this MCP entry. If yes, document *what* it provides that Bash/direct calls can't.
3. **Does insforge add anything this project actually needs?** If no — delete `.mcp.json` entirely, delete `.insforge/` folder, and remove insforge from `package.json`/`pyproject.toml` deps.
4. **If we re-enable anything, the insforge API_KEY MUST use env var expansion**, not a hardcoded value. Pattern: `"API_KEY": "${INSFORGE_API_KEY}"` — Claude Code supports env substitution in `.mcp.json`.

## How to re-enable something

Edit `.claude/settings.local.json`:
- Move server name from `disabledMcpjsonServers` to `enabledMcpjsonServers`.
- Restart claude.

## How to delete `.mcp.json` entirely

If you confirm none of these are needed:
1. Delete `.mcp.json` and `.mcp.json.bak-20260424`
2. Delete this file (`.mcp.md`)
3. Delete the `disabledMcpjsonServers` list from `.claude/settings.local.json`
4. (Optional) Delete `.insforge/` folder if insforge is truly gone
5. Git commit with message: `chore: remove unused MCP server scaffolding`

## Related docs

- `docs/decisions/2026-04-24-claude-code-harness-cleanup.md` — full context of the 2026-04-24 cleanup session
- `.mcp.json.bak-20260424` — pre-cleanup backup of `.mcp.json` (contains old leaked key — do not restore without rotating again)
