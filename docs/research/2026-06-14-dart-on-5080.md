# DART on the RTX 5080 — Phase 1 setup reality report

**Date started:** 2026-06-14 · **Continued:** 2026-06-20 · **Phase 1 COMPLETE:** 2026-06-22
**Stage 3 plan:** [`docs/plans/2026-06-14-stage3-ai-motion.md`](../plans/2026-06-14-stage3-ai-motion.md) (Phase 1.3 deliverable)
**Box:** Windows RTX 5080 16 GB → WSL2 Ubuntu-24.04 (kernel 6.6), reached from the Mac via `ssh rtx5080`
**Env:** Miniconda env `dart` (Python 3.10.20), torch **2.11.0+cu128** (Blackwell sm_120, `cuda_available=True`)

---

## TL;DR — ✅ PHASE 1 COMPLETE: DART generates motion on the RTX 5080

DART now **runs end-to-end on the 5080** and generated a real text→motion clip. The Phase-1 gate is fully met:
one recognizable-motion `.npz` on disk **plus** measured VRAM/latency on *our* hardware.

**Measured on the RTX 5080** (`mld.rollout_mld`, prompt `"walk in circles*20"`, guidance 5.0, 10 diffusion steps):
- **VRAM: 2.8 GB peak (2826 MiB of 16 GB)** — fits with ~13 GB headroom. The "won't fit 16 GB?" risk is dead.
- **Latency: 20-primitive diffusion rollout in ~1.3 s (~17 it/s steady).** Full run 15.8 s wall incl. *one-time*
  model load + CLIP ViT-B/32 (338 MB) download + SMPL-X mesh export. Generation itself is far faster than realtime.
- **Blackwell sm_120 + torch 2.11 cu128 ran the whole pipeline** (denoiser + MVAE + CLIP + SMPL-X export). No CUDA drama.
- **Output:** `sample_0_smplx.npz` — `poses (162, 165)` SMPL-X axis-angle + `trans (162, 3)`, 30 fps (5.4 s), no NaN,
  root translates through space (genuine locomotion). **This is exactly the axis-angle format Phase 2's `dart_to_glb.py` consumes.**

**Getting here required:** Chris supplying the MPI-gated SMPL-X body model (the one true user step — login/license);
Claude did everything else over SSH — deps, the pytorch3d transforms-shim, a numpy-2 source patch (6 files), placing
the body model + checkpoints + normalization data, and running/measuring the demo. **SMPL-H turned out NOT needed for
the default BABEL/SMPL-X demo** (only the HML3D variant uses it).

---

## What got done autonomously this session (no user involvement)

| Item | Status | Detail |
|---|---|---|
| SSH → WSL → GPU passthrough | ✅ confirmed | `nvidia-smi` in WSL shows RTX 5080 / 16303 MiB; torch matmul on sm_120 OK |
| DART repo on box | ✅ present | `/root/DART` (also `/mnt/c/dev/DART` on the Windows side) |
| Core pip deps | ✅ installed | smplx, openai-CLIP, einops, hydra-core, omegaconf, tyro, trimesh, transformers, pytorch-lightning, torch-dct, torchmetrics, scipy, imageio, loralib, rich, tqdm, tornado, matplotlib, pyrender, moviepy, tensorboard, fvcore, iopath, gdown — **25/25 import clean** |
| spacy | ✅ installed | Modern **spacy 3.8.14** + `en_core_web_sm` (old 2.3.4 pin dropped; usage is just `spacy.load()` + `nlp(sentence)`, fully 3.x-compatible) |
| pytorch3d | ✅ **shim** | Full source build fails (see below); vendored a **transforms-only shim** — pure-PyTorch rotation math, no `_C`. Verified: `axis_angle_to_matrix`, `matrix_to_quaternion`, `quaternion_to_axis_angle` all execute. This is exactly what the rollout path **and** Phase 2 rotation-injection need. |
| Model checkpoints | ✅ downloaded + placed | denoiser `checkpoint_300000.pt` (82 MB, 23.13 M params, verified loads under torch 2.11) + mvae `checkpoint_200000.pt` (48 MB) + their `args.yaml` — placed in the canonical tree; `args.yaml` confirms `mvae_path: ./mvae/mvae_fps_clip/checkpoint_200000.pt` (placement matches). |
| Body-model dirs | ✅ pre-created | `data/smplx_lockedhead_20230207/models_lockedhead/{smplx,smplh}/`. |
| **SMPL-X body model** | ✅ **placed + verified** | Chris downloaded `smplx_lockedhead_20230207.zip` (392 MB, the "removed head bun / locked head" NPZ); Claude scp'd → extracted → `models_lockedhead/smplx/SMPLX_{NEUTRAL,MALE,FEMALE}.npz`. `smplx.build_layer()` loads it. |
| SMPL-H body model | ⏭ **not needed for demo** | Only the HML3D variant uses SMPL-H (`.pkl`). The default BABEL/SMPL-X `run_demo` never references it. (Chris also grabbed `smplh.tar.xz` for later HML3D work.) |
| numpy-2 source patch | ✅ done | DART used removed `np.float`/`np.int`/… aliases → patched 6 files to builtins (numpy's own recommended safe transform). Cleared the last import error. |
| `data/seq_data_zero_male` (norm stats) | ✅ placed | `mean_std_h2_f8.pkl` + text-embedding dict by file-ID into the canonical dir + `data/stand.pkl`. Demo loaded `mean_std_h2_f8.pkl` ✓. |
| **DART demo run + VRAM/latency** | ✅ **MEASURED** | 2.8 GB peak VRAM, ~1.3 s for 20-primitive rollout, output `.npz` valid. See TL;DR. |

### pytorch3d build failure (why the shim)

`pip install git+pytorch3d` with `--no-build-isolation FORCE_CUDA=0` compiles the CPU C++ extension, but the
link step fails: the first object (`gather_scatter_cpu`) errors under the env's toolchain and ninja aborts, leaving
the linker unable to find any `.o` files (`ld: cannot find ...gather_scatter_cpu.o`). This is the Blackwell/new-stack
C++ friction the plan flagged. Rather than spiral on it (hypothesis limit), I confirmed by grep that **DART's
text→motion rollout uses only `pytorch3d.transforms`** (`from pytorch3d import transforms`; `pytorch3d.structures`
appears only in scene-collision code, NOT `run_demo`). `pytorch3d.transforms` is pure PyTorch — no compiled `_C`.
So I vendored just `pytorch3d/transforms` + `pytorch3d/common` (sparse-checkout from upstream) into site-packages
with a minimal `__init__.py` that does **not** import `_C`.

- **Adequate for:** the core text→motion rollout (`mld.rollout_demo` / `mld.rollout_mld`) and Stage-3 Phase-2
  axis-angle → quaternion → normalized-VRM conversion.
- **Not adequate for:** scene-interaction demos (`mld.optim_scene_mld`) that touch `pytorch3d.structures`/`_C`.
  If those are needed later, build pytorch3d properly (likely: install a matching CUDA dev toolkit + nvcc, or use
  an older C++ standard / pinned commit).

---

## THE BLOCKER — SMPL-X / SMPL-H body models (needs the user)

`utils/smpl_utils.py` runs this **at import time** (top of module):

```python
from config_files.data_paths import *            # body_model_dir = data/smplx_lockedhead_20230207/models_lockedhead/
body_model_dict = {
    'male':   smplx.build_layer(body_model_dir, model_type='smplx', gender='male',   ext='npz', num_pca_comps=12),
    'female': smplx.build_layer(body_model_dir, model_type='smplx', gender='female', ext='npz', num_pca_comps=12),
}
```

Without the `.npz` files this raises `ValueError: Unknown model type ...` and **every** demo import dies there.

### What the user needs to provide (one-time, login-gated)

Both downloads require a free **MPI account** (login + accept the non-commercial research license):

1. **SMPL-X** — <https://download.is.tue.mpg.de/download.php?domain=smplx&sfile=smplx_lockedhead_20230207.zip>
   (account at `smpl-x.is.tue.mpg.de`)
2. **SMPL-H** — <https://download.is.tue.mpg.de/download.php?domain=mano&resume=1&sfile=smplh.tar.xz>
   (account at `mano.is.tue.mpg.de`; needs a PKL conversion step — see DART README link)

### Exact drop-in layout on the box (dirs already created)

```
/root/DART/data/smplx_lockedhead_20230207/models_lockedhead/
├── smplx/
│   ├── SMPLX_FEMALE.npz
│   ├── SMPLX_MALE.npz
│   └── SMPLX_NEUTRAL.npz
└── smplh/
    ├── SMPLH_FEMALE.pkl
    └── SMPLH_MALE.pkl
```

The plan's chosen path is **"Claude drives the download via browser"** — feasible once the user is logged into the
two MPI sites in a Chrome that Claude can reach over CDP (precedent: `tools/mixamo_grab.mjs`). Alternatively the
user downloads the two archives manually and Claude unpacks + places them over SSH.

### ⚠ Commercial-license flag (unchanged)

SMPL-X / SMPL-H and the AMASS/BABEL/HumanML3D training data are **non-commercial research licenses**. Fine for
**prototyping**; a shipped commercial product needs commercial SMPL licensing (Meshcapade) + a data-license review.
This applies to essentially every SMPL-output motion model (DART/EMAGE/MoMask…), not just DART.

---

## Agentic-access notes (reusable)

- Reach the box: `ssh rtx5080 "<cmd>"` (default remote shell is **cmd.exe**).
- Run anything in WSL via the **base64-pipe** pattern to dodge quoting/pipe interception by cmd:
  `ssh rtx5080 "wsl -d Ubuntu-24.04 -u root -e bash -lc \"echo <BASE64> | base64 -d | bash\""`
  — note the **escaped double quotes** around the `bash -lc` arg: cmd.exe does **not** protect `|` inside
  *single* quotes, so single-quoting the pipeline silently breaks it.
- conda: `source /root/miniconda3/etc/profile.d/conda.sh; conda activate dart`.
- Detached jobs on the box **must** use `nohup` — a background `{ … } &` without nohup gets SIGHUP'd the instant
  the `bash -lc` returns (this is why the first checkpoint download died after one line).
- `gdown --folder` on the DART Drive folder **fails** (it chokes traversing the huge `policy_train/` tree of
  hundreds of RL `iter_*.pth`). Download the specific files **by file-ID** instead (`gdown <id> -O <path>`) — works
  cleanly, handles the large-file virus-scan confirm. File-IDs harvested from the folder enumeration are recorded
  in the plan status log.

---

## Next steps (in order)

1. **User provides SMPL-X + SMPL-H** per above (the only blocker). Place under `models_lockedhead/`.
2. Download `data/seq_data_zero_male` (norm stats) by file-ID/subfolder from the same public Drive folder.
3. `bash demos/run_demo.sh` → capture **real VRAM (`nvidia-smi` during run) + per-step latency** on the 5080 →
   fill in the "DART VRAM / latency" row above. Settles whether DART is a live runtime engine or a
   generate-then-cache tool on our box.
4. → **Phase 2:** SMPL-X axis-angle `.npz` → normalized-VRM GLB via `tools/dart_to_glb.py` + the Bug-2 harness,
   render-gated. The transforms shim already provides the rotation math this needs.
