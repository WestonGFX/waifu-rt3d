# Session 2026-05-30 — Psychology & Memory Engine (Embodied Girlfriend Re-Anchor)

## What was done

Repo-surgeon pass + a multi-wave build of the persistent psychology/memory engine,
re-anchored toward "embodied AI girlfriend first." Key framing decision: the engine the
PRDs asked for is **~80% already built** (Kokoro, tiered memory, bond, mood, proactive,
avatar director, voice duplex), so the work was **closing real gaps**, not a greenfield
rebuild. Two gaps turned out to already exist on inspection (vocab/facts injection;
voice-state emission) — caught by verify-before-building, avoiding parallel-system bloat.

### Shipped (branch `feat/psych-memory-engine`, atomic commits)
| Wave | Bundle | Schema | Commit |
|---|---|---|---|
| 0 | As-built design doc (`docs/research/2026-05-30-psychology-memory-engine-as-built.md`) | — | 2266… |
| A | Ritual memory — recurring-pattern layer (`relationship_rituals`, RitualManager, detect heuristic, injection ≥2 obs) | **v87** | 947e58f |
| B | Forget/privacy trust spine — soft-delete + suppression (no resurrection), per-memory privacy, cloud-exclusion filter, MemoryBrowser Forget+Lock | **v88** | f94907b |
| C | Listening cue — avatar attends when user speaks (duplex state → viewerStore → additive viewer.html cue) | — | b861fea |
| D | Psychology/memory debug dashboard — `/api/kokoro/debug/state` + tabbed KokoroDebugPanel (built by senior-dev agent) | — | 5318d62 |
| — | Agent team — 5 domain specialists + orchestration guide (`.claude/agents/`) | — | 978a60a |

### Decisions
- Schema migrations are serial (v86→v87→v88), never parallel-edited.
- viewer.html cue kept strictly additive, single-owner; **visual QA owed** (no headless avatar test) — checklist in `docs/2026-05-30-embodied-listening-cue.md`.
- D + safety-review parallelized via background agents (disjoint files).

## What's next
- **Cloud-privacy wiring (likely from safety review):** `tiered_memory.search(cloud_eligible=)` filter + `privacy_level` column exist, but confirm a real cloud send path passes `cloud_eligible=True` based on provider class. If not, the privacy guarantee isn't end-to-end — wire it.
- Greeting-on-return (deferred): route `proactive/generator` to app startup/session-resume.
- Visual QA of the listening cue on a live VRM.
- Backlog: fact/preference decay + contradiction resolution; Tier C trait→animation.

## Files changed
backend: `preflight.py` (v87+v88), `relationship/rituals.py` (new), `memory/tiered_memory.py`,
`server.py` (ritual inject + detect + forget/privacy endpoints + debug endpoint), tests
(`test_rituals.py`, `test_memory_forget.py`, `test_kokoro_debug_state_endpoint.py`).
frontend: `lib/api.ts`, `lib/kokoro.ts` (prior), `stores/viewerStore.ts`,
`hooks/useFullDuplexVoice.ts`, `components/MemoryBrowser.tsx`, `components/KokoroDebugPanel.tsx`,
`shared/viewer/viewer.html`, tests.
`.claude/agents/` — 5 new specialist agents + TEAM guide.

## Test counts
Backend 3057 → **3097** (+40). Frontend → **477** passing. tsc clean. Nothing pushed.
